# Streamlit Secrets
# Use this for local development. For production, set these as environment variables.

[api]
X_API_KEY = "piyu-rag-secret"
BACKEND_URL = "http://localhost:8000"

[ui]
THEME = "dark"
APP_TITLE = "Piyu's RAG AI Assistant"
